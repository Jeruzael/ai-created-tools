Implemented the next slice: v1.20 Baseline Creation And Comparison.

  What changed:

  - .gitignore already includes private/generated paths:
      - windows/security_check_records/
      - windows/security_reports/
      - windows/registry_backups/
      - windows/baselines/
      - windows/allowlist.local.json

  - Added local Security Check baselines stored under windows/baselines/.
  - Added API support to list baselines and create a baseline from a completed Security Check.
  - Added optional baseline comparison when starting a later Security Check.
  - Added UI for:
      - selecting a baseline before a run
      - creating a baseline from the current completed run
      - viewing baseline counts and comparison rows
      - filtering findings by new, changed, or unchanged

  - Added baseline comparison into the local report downloads.
  - Bumped app/docs metadata to v1.20.0.
  - Marked Slice 9 implemented in the Security Check roadmap.

  Verification passed:

  python -m py_compile .\windows\DiskUsageHtmlReport.py
  node --check runtime\dashboard-v120-script.js

  Also passed:

  - dashboard render check for v1.20
  - synthetic baseline comparison smoke test
  - /api/security-baselines endpoint check

  The updated v1.20 dashboard is running here:

  http://127.0.0.1:8767/

  Ports 8765 and 8766 still have older dashboard instances listening, so use 8767 for this version. Next roadmap
  slice is Slice 10: Advanced Review Collectors.