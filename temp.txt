Implemented Slice 2: Security Check job lifecycle.

  What changed:

  - Added backend lifecycle API:
      - POST /api/security-checks
      - GET /api/security-checks/active
      - POST /api/security-checks/cancel
      - GET /api/security-checks/{check_id}

  - Added local lifecycle records under windows/security_check_records.
  - Security Check now starts after acknowledgement, shows progress, can be cancelled, saves completed/cancelled
    records, and blocks Exit App while running.

  - Disk scans and Security Checks now block each other while one is active.
  - Updated UI polling/rendering for Security Check status, steps, skipped collector notes, and local record details.
  - Updated docs/version to v1.13.0 / v1.13.

  Key files:

  - windows/DiskUsageHtmlReport.py:22
  - windows/Windows_Disk_Usage_Dashboard_AppRequirements_v1.13_2026-06-18.md:1
  - README.md:1
  - windows/README.md:1
  - windows/dashboard_version.json:1

  Verification passed:

  - python -m py_compile windows\DiskUsageHtmlReport.py
  - Restarted server at http://127.0.0.1:8765/, PID 20420
  - API reports app_version: 1.13.0
  - Rejected Security Check start without acknowledgement.
  - Started Security Check with acknowledgement.
  - Confirmed shutdown is blocked while Security Check is running.
  - Cancelled a running Security Check and confirmed a cancelled record was saved.
  - Ran a completed Security Check lifecycle and confirmed local record saved.
  - Served JS syntax check passed: script_parse=ok
  - Legacy --scan-once still works.

  Important limitation: this still does not collect registry, Defender, scheduled tasks, browser policies, files,
  hashes, logs, WMI, or Sysmon. Next slice should be Standard Review Collector Set A.