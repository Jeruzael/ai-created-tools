 Implemented the next slice: Security Check v1.16, Slice 5 File Verification.

  What changed:

  - Added read-only file verification after Standard Review collectors.
  - Extracts referenced file paths from startup entries, scheduled tasks, services, Defender exclusions, and command
    evidence.

  - For existing files, records:
      - existence
      - Authenticode signature status
      - signer details when available
      - SHA-256 hash
      - created/modified timestamps
      - file size

  - Missing referenced files are marked File Missing.
  - Unsigned/problematic signatures are review signals, not malware verdicts.
  - Signature/hash failures are recorded as unavailable/skipped details instead of crashing the check.
  - Security Check summary now includes Unsigned Files.
  - File Verification is now an active category in the UI and links users to the existing Verification Guide.

  Verification passed:

  - python -m py_compile windows\DiskUsageHtmlReport.py
  - Fresh v1.16 Security Check completed with 421 findings.
  - File Verification produced 95 findings.
  - 88 referenced files had SHA-256 hashes.
  - Signature statuses included Valid and NotSigned.
  - Missing file behavior verified with File Missing.
  - API still rejects Security Check start without acknowledgement: 400.
  - Served browser JavaScript parses successfully.
  - Legacy --scan-once still works.

  The v1.16 server is running at:

  http://127.0.0.1:8765/